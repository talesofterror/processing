/* autogenerated by Processing revision 1289 on 2023-10-14 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class walk extends PApplet {

PVector canvas;
PVector worldOrigin; 
PVector object;
PVector movementVector = new PVector(1, 1);
PVector directionVector;
boolean edgeHit = false;

public void setup() {
  /* size commented out by preprocessor */;
  canvas = new PVector(width, height);
  object = new PVector(10, 10);
  worldOrigin = new PVector(0, 0);
}

public void draw() {
  background(100);
  stroke(255, 150, 0);
  strokeWeight(5);
  
  movingDot();
}

public void movingDot () {
  point(object.x, object.y);

  object.add(directionToggle);

}

public PVector directionToggle () {
  if (object.equals(canvas) | object.equals(worldOrigin)){
    return object.add(movementVector);
  } else {
    return object.sub(movementVector);
  }
}


  public void settings() { size(400, 400); }

  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "walk" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
